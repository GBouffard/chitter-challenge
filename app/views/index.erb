<!DOCTYPE html>
<html>
  <head>
    <title>Guillaume 's Chitter</title>
  </head>

  <body>
  This is Guillaume's Chitter
  <br><br>
  <% if current_user %>
    <a href="/peeps/new"><button>New Peep</button></a><br><br>
  <% else %>
    <a href="/users/new"><button>Sign Up</button></a>
    <a href="/sessions/new"><button>Sign In</button></a><br><br>
  <% end %>
    <div>
      <ul>
        <% @peeps.each do |peep| %>
          <li><%= peep.message %><br>
            posted by <%= peep.user.email %> on the <%= peep.date_time.strftime("%d/%m/%Y") %> at <%= peep.date_time.strftime("%H:%M") %><br>
            <a href="/peeps/<%= peep.id %>"><button>Update Peep</button></a>

            <form action="/peeps/<%= peep.id %>" method="post">
              <input type="hidden" name="_method" value="delete">
              <input type="submit" value="Delete Peep">
            </form>
          </li>
        <% end %>
      </ul>
    </div>
  </body>
</html>
